name: Publish 9-marketplace-shared

on:
  push:
    branches: [main]
    paths:
      - 'server/9-marketplace-shared/**'
      - '.github/workflows/publish-9-marketplace-shared.yml'  # ← also trigger when workflow itself changes
  pull_request:
    branches: [main]
    paths:
      - 'server/9-marketplace-shared/**'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    defaults:
      run:
        working-directory: ./server/9-marketplace-shared

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20.x
          registry-url: https://npm.pkg.github.com/

      - run: npm install

      - run: npm run build

      - run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}